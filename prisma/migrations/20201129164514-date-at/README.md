# Migration `20201129164514-date-at`

This migration has been generated by wabi91 at 11/30/2020, 1:45:14 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `Comment` ADD COLUMN     `createAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    ADD COLUMN     `updateAt` DATETIME(3) NOT NULL,
    MODIFY `userId` INT NOT NULL

ALTER TABLE `File` ADD COLUMN     `createAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    ADD COLUMN     `updateAt` DATETIME(3) NOT NULL

ALTER TABLE `Like` ADD COLUMN     `createAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    ADD COLUMN     `updateAt` DATETIME(3) NOT NULL

ALTER TABLE `Post` ADD COLUMN     `createAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    ADD COLUMN     `updateAt` DATETIME(3) NOT NULL

ALTER TABLE `User` ADD COLUMN     `avatar` VARCHAR(191) NOT NULL DEFAULT 'https://3.bp.blogspot.com/-qtEejOg1NHA/Xobmg2y_QeI/AAAAAAAAIVE/UFKPvpeHjKUqCEFOX8lT4MsKz-PwpEGJgCLcBGAsYHQ/s1600/default_user.png',
    ADD COLUMN     `createAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    ADD COLUMN     `updateAt` DATETIME(3) NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201129162928-wabigram..20201129164514-date-at
--- datamodel.dml
+++ datamodel.dml
@@ -2,18 +2,20 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id        Int @id @default(autoincrement())
+  id        Int @default(autoincrement()) @id
   username  String @unique
+  // Default 이미지 주소 변경 필요
+  avatar    String @default("https://3.bp.blogspot.com/-qtEejOg1NHA/Xobmg2y_QeI/AAAAAAAAIVE/UFKPvpeHjKUqCEFOX8lT4MsKz-PwpEGJgCLcBGAsYHQ/s1600/default_user.png")
   email     String @unique
   firstName String @default("")
   lastName  String
   bio       String
@@ -21,38 +23,50 @@
   likes     Like[]
   comments  Comment[]
   followers User[] @relation("UserFollows", references: [id])
   following User[] @relation("UserFollows", references: [id])
+  createAt  DateTime @default(now())
+  updateAt  DateTime @updatedAt
 }
 model Post {
-  id        Int @id @default(autoincrement())
+  id        Int @default(autoincrement()) @id
+  author    User @relation(fields: [authorId], references: [id])
+  authorId  Int
   location  String
   caption   String
-  author    User @relation(fields: [authorId], references: [id])
-  authorId  Int
   files     File[]
   likes     Like[]
   comments  Comment[]
+  createAt  DateTime @default(now())
+  updateAt  DateTime @updatedAt
 }
 model Like {
-  id        Int @id @default(autoincrement())
+  id        Int @default(autoincrement()) @id
   user      User @relation(fields: [userId], references: [id])
   userId    Int
   post      Post @relation(fields: [postId], references: [id])
   postId    Int
+  createAt  DateTime @default(now())
+  updateAt  DateTime @updatedAt
 }
 model Comment {
-  id        Int @id @default(autoincrement())
+  id        Int @default(autoincrement()) @id
   text      String
+  user      User @relation(fields: [userId], references: [id])
+  userId    Int
   post      Post @relation(fields: [postId], references: [id])
   postId    Int
+  createAt  DateTime @default(now())
+  updateAt  DateTime @updatedAt
 }
 model File {
-  id        Int @id @default(autoincrement())
+  id        Int @default(autoincrement()) @id
   url       String
   post      Post @relation(fields: [postId], references: [id])
   postId    Int
+  createAt  DateTime @default(now())
+  updateAt  DateTime @updatedAt
 }
```


